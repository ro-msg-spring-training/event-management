import { CategoryCardItem } from '../types/TicketType';
/*export enum ActionTypes {
  ADD_CATEGORY_CARD = " ADD_CATEGORY_CARD ",
  REMOVE_CATEGORY_CARD = "REMOVE_CATEGORY_CARD",
  ADD_EMPTY_CATEGORY_CARD = "ADD_EMPTY_CATEGORY_CARD",
  LOAD_CARDS = "LOAD_CARDS",
  SET_CARDS = "SET_CARDS",
}

export class AddCategoryCardAction {
  public readonly type = ActionTypes.ADD_CATEGORY_CARD;
  public card: JSX.Element;

  constructor(card: JSX.Element) {
    this.card = card;
  }
}

export const addCategoryCard = (card: JSX.Element): AddCategoryCardAction => {
  return {
    type: ActionTypes.ADD_CATEGORY_CARD,
    card: card,
  };
};

export class AddEmptyCategoryCardAction {
  public readonly type = ActionTypes.ADD_EMPTY_CATEGORY_CARD;
}

export const addEmptyCategoryCard = (): AddEmptyCategoryCardAction => {
  return {
    type: ActionTypes.ADD_EMPTY_CATEGORY_CARD,
  };
};

export class SetCardsAction {
  public readonly type = ActionTypes.SET_CARDS;
  public cards: CategoryCardItem[];

  constructor(cards: CategoryCardItem[]) {
    this.cards = cards;
  }
}

export const setCards = (cards: CategoryCardItem[]): SetCardsAction => {
  return {
    type: ActionTypes.SET_CARDS,
    cards: cards,
  };
};

export class LoadCardsAction {
  public readonly type = ActionTypes.LOAD_CARDS;
  public cards: CategoryCardItem[];

  constructor(cards: CategoryCardItem[]) {
    this.cards = cards;
  }
}

export const loadCards = (cards: CategoryCardItem[]): LoadCardsAction => {
  return {
    type: ActionTypes.LOAD_CARDS,
    cards: cards,
  };
};

export class RemoveCategoryCardAction {
  public readonly type = ActionTypes.REMOVE_CATEGORY_CARD;
  public id: number;

  constructor(id: number) {
    this.id = id;
  }
}

export const removeCategoryCard = (id: number): RemoveCategoryCardAction => {
  return {
    type: ActionTypes.REMOVE_CATEGORY_CARD,
    id: id,
  };
};

export type CategoryPageAction =
  | AddCategoryCardAction
  | AddEmptyCategoryCardAction
  | LoadCardsAction
  | RemoveCategoryCardAction
  | SetCardsAction;
*/
