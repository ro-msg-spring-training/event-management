export interface UserEventList {
    id: number,
    title: string,
    location: string,
    startDate: Date,
    endDate: Date,
    startHour: string,
    endHour: string,
    rate: number,
    images: string[]
}