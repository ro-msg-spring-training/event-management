import { createMuiTheme } from '@material-ui/core';

const mytheme = createMuiTheme({
    palette: {
      primary:{
        main: "#a01441",
      },
      secondary: {
       main: "#001427",
     },
    },
   
 });
 export default mytheme