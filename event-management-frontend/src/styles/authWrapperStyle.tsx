import { makeStyles, Theme } from "@material-ui/core";

export const useStylesAuth = makeStyles((theme: Theme) => ({
  languageButton: {
    float: "right",
  },

  authHeader: {
    height: "2em",
  },
}));
